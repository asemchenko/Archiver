сжатие данных (входной файл, выходной файл,):
	Инициализация словаря всеми возможными буквами (байтами)
	Выполнить кодирование 16 битными кодами
	Если (входной файл еще не закончился) && (пора переходить на 32 битные коды):
		Выполнить кодирование 32 битными кодами
	Если размер закодированных данных меньше чем незакодированных:
		Записать в архив закодированные данные
		Установить флаг в начале архива о том что данные былы закодированны
	Иначе:
		Записать в архив незакодированные данные
		Установить флаг в начале архива о том что данные не были закодированны
	Закрыть входной файл
кодирование 16 битными кодами(входной файл, словарь):
	word := первая буква файла
	Пока !file.eof() и достаточно 16 битных кодов:
		считать из файла символ symb
		Если word + symb есть в словаре:
			word := word + symb
		Иначе:
			Добавить в словарь word
			записать в массив кодов код для word 
			word := symb
	Если еще не пора переходить на 16 битные коды:
		Записать в массив кодов код для word
	Иначе:
		Разбить код для word на два кода по 16 бит и записать их в массив кодов
кодирование 32 битными кодами(входной файл, словарь):
	word := первая буква файла
	Пока !file.eof():
		считать из файла символ symb
		Если word + symb есть в словаре:
			word := word + symb
		Иначе:
			Добавить в словарь word
			записать в массив кодов код для word 
			word := symb
	Записать в массив кодов код для word

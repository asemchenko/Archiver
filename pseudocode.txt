сжатие данных (входной файл, выходной файл,):
	Инициализация словаря всеми возможными буквами (байтами)
	code_lenght := 9
	word := первый байт входных данных
	Считать байт из входного файл в current_symb
	Если входной файл еще не закончился:
		Пока не использованы все коды длины code_lenght и файл не окончен:
			// выполняем кодирование
			Если (word + current_symb) есть в словаре:
				word := word + current_symb
			Иначе:
				Добавить в словарь комбинацию (word + current_symb)
				Записать в result код для word (длина этого кода будет code_lenght)
				word := current_symb
		code_lenght += 1
	Если размер закодированных данных меньше чем незакодированных:
		Записать в архив закодированные данные
		Установить флаг в начале архива о том что данные былы закодированны
	Иначе:
		Записать в архив незакодированные данные
		Установить флаг в начале архива о том что данные не были закодированны
	Закрыть входной файл
Запись битов в вектор результата(число number, кол-во битов count_bites):
	//Пусть в буффере есть место для n битов
	Считать старшые n бит из number и записать их в буффер
	Записать содержимое буффера в вектор результата
	Считать сколько возможно оставшихся байт числа number и записать их в вектор результата
	В старшие разряды буффера записать оставшиеся от числа number биты
	Вернуть кол-во незанятых битов в буффере
